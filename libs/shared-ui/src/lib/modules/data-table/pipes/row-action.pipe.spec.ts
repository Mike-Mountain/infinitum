import { RowActionPipe } from './row-action.pipe';

describe('RowActionPipe', () => {
  it('create an instance', () => {
    const pipe = new RowActionPipe();
    expect(pipe).toBeTruthy();
  });
});
