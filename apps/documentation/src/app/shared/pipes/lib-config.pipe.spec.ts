import { LibConfigPipe } from './lib-config.pipe';

describe('LibConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new LibConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
